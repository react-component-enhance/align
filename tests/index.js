require('core-js/es6/map');
require('core-js/es6/set');
const expect = require('expect.js');
const Align = require('../');

describe('rce-align', () => {
  it('exists', () => {
    expect(Align).to.be.ok();
  });
});
